<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="buttonDiv">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/">Scraped Articles<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/scrape">Scrape New Articles!</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/articles/saved">Saved Articles</a>
            </li>
        </ul>
    </div>
</nav>

<h1>Scrape Articles</h1>

{{#each result}}
<div class="summary-box">
    <h4>
        <a href={{this.link}}>{{this.title}}</a>
    </h4>
    {{this.summary}}
    <br>
    <button class="save-Button" data-id={{this._id}} type="text">Save</button>
</div>
{{/each}}
</div>